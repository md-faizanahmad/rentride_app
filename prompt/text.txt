signup form with name,email,phone,password(like create a account using name,phone,email,password) - its responsive ,animated , and professional ui and ux -add loading loader icon also if signup button take time and mui skeleton on delay---import Link from "next/link";
import { Phone } from "lucide-react";

export default function SignupServer() {
  return (
    <section className="min-h-screen flex items-center justify-center bg-gradient-to-r from-gray-300 via-white to-purple-100 p-4">
      <div className="max-w-md w-full bg-white/80 backdrop-blur-md rounded-2xl shadow-xl border border-gray-100/50 p-8">
        <h1 className="text-3xl font-bold text-center text-gray-800 mb-6">
          Sign Up for RentRide
        </h1>
        <p className="text-center text-gray-600 mb-8">
          Create an account to start renting or listing vehicles.
        </p>

        <div className="space-y-4">
          <div id="google-signup-placeholder" />

          <button
            className="w-full cursor-pointer flex items-center justify-center bg-gray-200 text-gray-800 py-3 px-4 rounded-xl shadow-md hover:bg-gray-300 transition-colors duration-200"
            aria-label="Sign up with phone number (coming soon)"
            disabled
          >
            <Phone className="w-5 h-5 mr-2" aria-hidden="true" />
            Sign up with Phone (Coming Soon)
          </button>
        </div>

        <p className="mt-6 text-center text-sm text-gray-600">
          Already have an account?{" "}
          <Link
            href="/auth/accounts"
            className="text-blue-600 hover:underline font-medium"
          >
            Log in
          </Link>
        </p>
      </div>
    </section>
  );
}
-----"use client";

import { motion, Variants } from "framer-motion";
import SignupServer from "./SignUpServer";

export default function SignupClient() {
  const containerVariants: Variants = {
    hidden: { opacity: 0, y: 50 },
    visible: {
      opacity: 1,
      y: 0,
      transition: {
        duration: 0.5,
        ease: [0.25, 0.1, 0.25, 1],
        staggerChildren: 0.1,
      },
    },
  };

  return (
    <motion.div
      variants={containerVariants}
      initial="hidden"
      animate="visible"
      className="motion-wrapper"
    >
      <SignupServer />
    </motion.div>
  );
}
